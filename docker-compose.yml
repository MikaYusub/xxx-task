services:
  firebase:
    build:
      context: .
      dockerfile: docker/firebase/Dockerfile
    command: sh -c "npm install && npm run build --workspaces --if-present && npx firebase emulators:start --project demo-local --only auth,firestore,functions,pubsub"
    depends_on:
      - config-service
      - inference-server
    env_file:
      - .env
    environment:
      FIRESTORE_EMULATOR_HOST: 127.0.0.1:8080
      FIREBASE_AUTH_EMULATOR_HOST: 127.0.0.1:9099
    ports:
      - "4000:4000"
      - "5001:5001"
      - "8080:8080"
      - "8085:8085"
      - "9099:9099"
    volumes:
      - .:/workspace
      - firebase_node_modules:/workspace/node_modules
    working_dir: /workspace

  config-service:
    build:
      context: services/config-service
    env_file:
      - .env
    ports:
      - "3000:3000"

  inference-server:
    build:
      context: services/inference-server
    env_file:
      - .env
    environment:
      FIRESTORE_EMULATOR_HOST: firebase:8080
      GOOGLE_CLOUD_PROJECT: demo-local
      INFERENCE_MODE: real
      OUTPUT_DIR: /app/outputs
      LORA_CACHE_DIR: /app/cache/lora
    ports:
      - "8000:8000"
    volumes:
      - ./outputs:/app/outputs
      - ./cache:/app/cache
      - ./cache/huggingface:/root/.cache/huggingface

  publisher:
    image: node:20-bookworm
    profiles:
      - tools
    env_file:
      - .env
    environment:
      FIRESTORE_EMULATOR_HOST: firebase:8080
      FIREBASE_AUTH_EMULATOR_HOST: firebase:9099
    volumes:
      - .:/workspace
      - publisher_node_modules:/workspace/node_modules
    working_dir: /workspace

volumes:
  firebase_node_modules:
  publisher_node_modules:
